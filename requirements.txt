# Crew Agent AI - runs locally, no API key needed.
flask>=3.0.0

# Production WSGI server used in the cloud (Linux). Gunicorn does not exist on
# Windows, where you run "python app.py" instead - hence the marker.
gunicorn>=21.2.0; sys_platform != "win32"

# OPTIONAL: the CrewAI crew wrapper. Every answer is produced by the local
# brain in chatbot.py, so the app behaves identically without these packages.
# Skipping them keeps the cloud build (Render free plan) small, fast and
# within its 512 MB memory limit. To run the full CrewAI crew locally, install:
#   pip install "crewai==0.11.2" "langchain-core>=0.1.42,<0.2"